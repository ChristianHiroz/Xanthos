{% trans_default_domain 'FOSUserBundle' %}

<div class="container" style="padding:25px;">
    <div class="row">
        <div class="col-md-12" style="text-align: center; height:230px;">
            <p>{{ 'profile.show.username'|trans }}: {{ user.username }}</p>
            <p>{{ 'profile.show.email'|trans }}: {{ user.email }}</p>
            <br/>
            <a class="shopping-item" style="float:none; width:10%;" href="{{ path('fos_user_profile_edit') }}">Modifier</a>
        </div>
    </div>
</div>
